(function () {
  // Screen-specific logic for Sign In screen (placeholder)
  // Hook up click handlers if needed.
  const signInBtn = document.querySelector('.big-button');
  if (signInBtn) {
    signInBtn.addEventListener('click', () => {
      console.log('Sign In clicked');
    });
  }
})();
